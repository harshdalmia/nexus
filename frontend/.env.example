# Copy to `.env.local`. Without this the client defaults to http://127.0.0.1:8000.
#
# The frontend works with no backend at all: if the engine cannot be reached it replays
# its bundled demo scenarios and labels them as demo data in the UI.
VITE_API_BASE_URL=http://127.0.0.1:8000
